import { FC } from "react"
import { Layout } from "components/layouts"
import { Home } from "components/pages"

const App: FC = () => {
  return (
    <Layout>
      <Home />
    </Layout>
  )
}

export default App
